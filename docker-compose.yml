version: '3'
services:
  app:
    build: .
    command: bash
    tty: true
    stdin_open: true
    volumes:
      - type: bind
        source: .
        target: /app
  test-container:
    build: ./test-container
    command: /bin/bash
    stdin_open: true
    tty: true